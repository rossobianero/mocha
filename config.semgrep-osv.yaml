repos:
  - name: VulnerableApp
    local_path: ./repos/VulnerableApp
    scanners:
      - plugin: SemgrepPlugin
        config:
          use_docker: false     # <— key change
          rules:
            - p/owasp-top-ten
            - p/r2c-security-audit
          extra_args:
            - --timeout
            - "180"
      - plugin: OSVScannerPlugin
        config:
          use_docker: false     # <— key change
          extra_args:

